{
  "name": "my-electron-app",
  "version": "1.0.0",
  "description": "A simple Electron application",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "package-win": "electron-packager . Weather-App --platform=win32 --arch=x64 --out=dist --overwrite"
  },
  "author": "Ravindu Wickramage",
  "license": "ISC",
  "devDependencies": {
    "electron": "^33.2.1",
    "electron-packager": "^17.1.2"
  }
}
